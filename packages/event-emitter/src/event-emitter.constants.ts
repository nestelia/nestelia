/**
 * Injection token for the EventEmitterService instance.
 *
 * @publicApi
 */
export const EVENT_EMITTER_TOKEN = "EVENT_EMITTER_TOKEN";

/**
 * Metadata key used by @OnEvent to store handler registrations on a class.
 *
 * @internal
 */
export const ON_EVENT_METADATA = "__onEvent__";
